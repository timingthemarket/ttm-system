namespace TTM.Shared.Constants;

public enum Direction
{
    Low = -1,
    High = 1,

    /// <summary>
    /// If custom implementation needed to determine what is a good value
    /// </summary>
    Other = 0
}